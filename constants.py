

class Var:
    """Keeping constant(same)/ variables for each class in superclass(here)"""
    def __init__(self, x, y, color, radius):
        self.x = x
        self.y = y
        self.color = color
        self.radius = radius
